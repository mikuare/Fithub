# Tests always run against the in-browser local backend — never a live
# Supabase project. Vite loads this file in test mode on top of .env, and the
# empty values make backend() fall back to LocalBackend.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
